package com.example.SpringGroupBB.constant;

// 문의현황 관련 enum.
// RESOLVING(처리중), UNRESOLVABLE(처리불가), RESOLVED(처리완료), ANSWER(답변), DELETE(삭제).
public enum Progress {
  RESOLVING,
  UNRESOLVABLE,
  RESOLVED,
  ANSWER,
  DELETE
}
